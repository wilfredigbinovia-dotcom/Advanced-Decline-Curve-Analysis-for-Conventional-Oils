# Decline Curve Workbench -- Python requirements
#
#   pip install -r requirements.txt
#
# Streamlit Community Cloud installs from this file, so the app's own
# dependencies are listed here too. If you only want the dca.py library and
# its CLI, the first three lines are all you need.
#
# numpy and scipy are the only hard requirements: scipy supplies the
# Nelder-Mead simplex the fitter runs on. pandas is used for CSV loading and
# for the material-balance result tables.
#
# Lower bounds are the versions this was developed and tested against.
# Nothing is pinned to an exact version; the APIs used are long-stable.

numpy>=1.24
scipy>=1.10
pandas>=2.0

# dca.py --plot and plot_forecast() only
matplotlib>=3.7

# streamlit_app.py only
streamlit>=1.30
plotly>=5.18
